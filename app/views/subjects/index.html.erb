<h1>III. Temas</h1>
<% if current_user %><%= link_to 'Criar tema', new_subject_path %><% end %>

<br/>
<%= page_entries_info @subjects %>
<hr/>
<% @subjects.each do |s| %>
  <div>
    <h2 style="color:#333333"><%= link_to s.title, subjects_path(s) %></h2>
  </div>
  <hr/>
<% end %>

<%= will_paginate @subjects %>

