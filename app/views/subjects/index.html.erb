<% content_for :subject_content do %>
  <% if @subjects.size > 0 %>
    <% @subjects.each do |s| %>
      <div class="list_article">
        <h3 style="color:#333333"><%= link_to s.title, subject_path(s) %></h3>
      </div>
    <% end %>
  <% else %>
    <h3>Nenhum tema econtrado</h3>
  <% end %>  
<% end %>  
