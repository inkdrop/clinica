<% content_for :subject_options do %>
  <%= link_to "× Excluir tema", @subject, :class => "btn danger", :confirm => 'Tem certeza que deseja excluir este registro?', :method => :delete if @subject.id != nil && current_user %>
<% end %>
  
<% content_for :subject_content do %>
  
  <h2><%=t :edit_subject %></h2>
  <hr/>
  <%= render 'form' %>

  <%= link_to 'Show', @subject %> |
  <%= link_to 'Back', subject_path(@subject) %>
<% end %>
