<%= content_for :topic_content do %>
  <h2><%=t :new_topic %> (<%= link_to @subject.title, subject_path(@subject) %>)</h2>

  <%= render 'form' %>

  <%= link_to 'Back', subject_path(@subject) %>
<% end %>
