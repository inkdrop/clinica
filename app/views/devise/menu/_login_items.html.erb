<% if user_signed_in? %>
  <li>
  Olá <%= current_user.name %> (<%= link_to('Logout', destroy_user_session_path) %>)        
  </li>
<% else %>
  <li>
  <%= link_to('Login', new_user_session_path)  %>  
  </li>
<% end %>
